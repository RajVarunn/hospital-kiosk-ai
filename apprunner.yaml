version: 1.0
runtime: nodejs16
build:
  commands:
    build:
      - cd backend
      - npm install
run:
  runtime-version: 16
  command: cd backend && npm start
  network:
    port: 3001
    env: PORT
  env:
    - name: NODE_ENV
      value: production